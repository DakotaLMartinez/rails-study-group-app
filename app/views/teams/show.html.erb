<h1><%= "#{@team.city} #{@team.name}" %></h1>
<p>Coach: <%= @team.coach %></p>
<%= link_to("Delete Team", team_path(@team), data: {method: :delete, confirm: "Are you sure you want to delete this team?"}) %>